# Fill in and rename to meta.txt before committing.
date=<RFC3339>
host=<build host>
kernel=<uname -r>
cpu=<cpu model>
git_commit=<short sha>
qemu_version=<qemu version>
mtools_version=<mcopy version>
sgdisk_version=<sgdisk version>
image_path=build/rush-linux.raw
image_sha256=<sha256>
instances_max=3

# Acceptance command:
#   tools/test-rollback.sh build/rush-linux.raw
# Must produce: PASS lines for (1) UKI boot, (2) >=3 rollback entries retained,
# (3) bad-kernel rollback recovery. transcript.log must contain all three.

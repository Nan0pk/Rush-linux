# Strategic Reassessment — 2026-06-15

> Manual run of `.github/workflows/reassess.yml`. Triggered by Claude (verifier, solo
> session with HUMAN), not the schedule. Deltas vs the 2026-06-14 snapshot are
> annotated inline. Living view: `docs/strategy/COMPASS.md`.

## Metrics (via GitHub API, collected 2026-06-15)

| Metric | 2026-06-14 | 2026-06-15 | Δ |
|---|---|---|---|
| Stars | 1 | 1 | — |
| Forks | 0 | 0 | — |
| Open issues | 30 | 26 | −4 (some resolved) |
| Open PRs | 3 | 0 | −3 (all merged) |
| Traffic views | n/a | 195 (4 unique) | first cycle with data |
| Traffic clones | n/a | 2,374 (571 unique) | first cycle with data |
| Commits to `main` since last | — | 62 | (counter reset) |

**Notable:** clone count (571 unique) is high relative to views (4 unique). Strong
signal that this repo is being pulled by agents / CI rather than read by humans.
Low star count is unchanged but expected for R&D phase.

## State of the WHY (Δ vs 2026-06-14)

The objective (Apple-class adaptive power/perf on Linux laptops, via `optid`)
**remains unchanged**. SPEC-northstar §0 is canon and was reinforced this cycle
by:
- PR #50 (Apple power stack analysis) — **merged**, research canon now on `main`.
- PR #51 (architecture + power orchestrator reviews) — **merged**.
- PR #52 (SPEC-northstar + agent protocol addendum) — **merged**.

**No silent pivot.** Anti-pivot contract holds. The 6-rung YAGNI ladder
(extracted from ponytail as `.agents/skills/yagni-ladder/`) is *informational
only* and does not override the WP evidence rule.

## Evidence gates (SPEC §6)

| Gate | 2026-06-14 | 2026-06-15 |
|---|---|---|
| LATENCY | CLOSED (PR #72) | **CLOSED** ✓ unchanged |
| ENERGY | OPEN | **CLOSED** (PR #75 + PR #79 trim merged, battery run evidence at `557673d`) |

**Both §6 gates are now green.** First complete evidence story in the project's
history. Caveats (carried forward from WP-ENERGY-PROBE verdict):

1. Dummy `optid --apply` shim was used in the battery dataset — proves the
   *probe*, not optid efficacy. Real optid-applied runs are a follow-up.
2. psi-cpu/psi-io `samples` arrays are all-zero (4.4 M–4.5 M entries × 0) — likely
   sampler bug, recommend separate follow-up issue.

## New since 2026-06-14

### Merged
- **PR #75** (ENERGY gate) — code + battery evidence. **Energy gate CLOSED.**
- **PR #78** (agent-bus skill) — stashed the multi-agent protocol as
  `.agents/skills/agent-bus/` for later reuse. Currently **suspended**.
- **PR #79** (297 MB trim) — `samples` arrays reduced to null + `meta` block.
  311 MB → ~12 KB.
- **PRs #50, #51, #52** (research + spec-northstar canon) — merged via `-X
  theirs` after end-of-file conflicts in `docs/docmap.toml` blocked the default
  strategy.
- **PR #80** (yagni-ladder skill) — extracted the 6-rung decision tree from
  ponytail into `.agents/skills/yagni-ladder/`. Standalone, opt-in.
- **PR #81** (WP-B1E workplan preservation) — saved a 433-line plan that lived
  only on an abandoned branch.

### Branch cleanup
15 → 8 branches. Kept: `main`, `agent-bus`, `graphify-data`. The 5-cycle cleanup
deleted 9 stale merged branches; 4 more branches (the abandoned research/WP
branches) were either merged-then-deleted or preserved for the workplan.

### Tooling additions
- `tools/trim-benchmark-samples.py` — companion to the trim PR.
- `.agents/skills/agent-bus/SKILL.md` + 4 templates + `tools/flip.py` (257 lines).
- `.agents/skills/yagni-ladder/SKILL.md` + 4 companion files (257 lines).

### Sub-bug surfaces opened
- All-zero `samples` array in psi-cpu/psi-io (sampler bug — follow-up).
- Branch protection + GitHub merge bot doesn't honor `-X theirs` (operational
  workaround: pre-resolve conflicts on the PR branch, then merge via web
  button).

## Third-person product outlook

**(a) Human user view.** No installable image or demo yet. **This is the same
gap as the previous cycle.** The new asset is the **complete evidence story**
(latency + energy, with mock-free data). If a renderable chart or short
"what this proves" section lands in the README, this gap closes without any
code change.

**(b) AI-agent / contributor view.** **Improved.** Canon (SPEC-northstar,
architecture, research) is on `main`. Two new skills (agent-bus, yagni-ladder)
make the repo more legible to agents. The 433-line WP-B1E workplan is preserved
in `docs/plans/`. A new agent can orient faster now.

## Reach / visibility

Unchanged from 2026-06-14: 1 star, 0 forks, no installable artifact, no rendered
evidence. The 571 unique clones signal that **agents/CI are engaging with the
repo**, not humans — the gap is on the human-visibility side, not the technical
side.

## Drift check

- **Direction drift:** none. The merged PRs all advance the same northstar.
- **Process drift:** none. WP evidence rule held across all merges. The
  agent-bus protocol worked as designed (Claude flagged the conflict-resolution
  needs; user merge decisions respected).
- **Scope drift:** none visible. Yagni-ladder is a *narrow* extraction (decision
  tree only); the lazy-senior-dev culture was *not* adopted.

## Verdict

**ON-TRACK** (R&D phase, evidence complete, canon merged, still pre-visibility).

The previous cycle's three course-corrections have all been actioned:
1. ✅ **Close the first full evidence story** — both §6 gates are now green.
2. ✅ **Get the canon onto `main`** — PRs #50/#51/#52 all merged.
3. ⏸ **Make the proof visible** — README chart / short "what this proves" section
   still not done.

## Course-corrections (1–3) for the next cycle

1. **Make the proof visible.** Render the latency + energy datasets as a small
   chart in `README.md`. This is the highest-leverage unblocker for human
   visibility. Effort: ~1 hour (script + screenshot or hand-curated table).
2. **File the all-zero `samples` sampler-bug issue.** This is an honest
   unknown flagged in the WP-ENERGY-PROBE verdict. Effort: ~30 min to file,
   unknown effort to fix.
3. **Decide on the WP-B1E workplan's worth.** It survived because Claude
   asked; was it worth preserving? If yes, integrate it into the WP planning
   docs; if no, archive it as historical.

## Files written this cycle

- `docs/strategy/reassessments/2026-06-15.md` — this file (snapshot).
- `docs/strategy/COMPASS.md` — refreshed living view.
- `.github/reassess-counter.txt` — reset to `baca74e9fc933e1d31abfefe2ee5110f600a7755`
  (current main HEAD).

## Counter reset

```
LAST_COUNTER:  f313147c65a529272f6d141b688bc610b13c746f
NEW_COUNTER:   baca74e9fc933e1d31abfefe2ee5110f600a7755
COMMITS:       62
```

Next scheduled fire: Monday 2026-06-22 06:00 UTC, or after ~10 more merges.

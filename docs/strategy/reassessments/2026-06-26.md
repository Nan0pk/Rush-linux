# Strategic Reassessment — 2026-06-26

> Dated snapshot of the 2026-06-26 strategic reassessment cycle. Living view:
> `docs/strategy/COMPASS.md`. Trigger: PR #174 (closed Dragnet-001 evidence
> debt) + this cycle's review. Supersedes the 2026-06-17 snapshot. Posted as
> a comment on issue #105.

## Metrics (auto-collected)

| Metric | 2026-06-17 | 2026-06-26 | Δ |
|---|---|---|---|
| Stars | 1 | 1 | — |
| Forks | 0 | 0 | — |
| Open issues | 1 | 1 | — |
| Open PRs | 0 | 2 (#175, #176) | +2 |
| Commits to `main` since last | — | 40+ | — |

No inbound issues or PRs from external contributors in this cycle.

## 1. WHY still true?

Yes. The SPEC-northstar objective — *an adaptive Linux distribution with one
explainable, reversible optimizer that proves its work* — is unchanged. The
v0.5 closure strengthens the anti-pivot contract: the project now has
end-to-end evidence (source → mkosi image → installable disk → boot →
rollback → signing) for the installable-system claim. No pivot pressure
detected.

## 2. New research / findings / what's been MISSED

- **Closed:** The Dragnet-001 evidence debt (issue #168) is closed as of
  PR #174, 2026-06-23. All 12 v0.3/v0.4/v0.5 exit criteria now carry
  committed transcripts. Dragnet-004 (2026-06-26) is the first GREEN report.
- **Missed earlier, now visible:** The project has zero published `rushbench`
  results. The README still says "No results published yet." v0.6's Phase D
  is the first time real hardware benchmarks land; v0.8 (Benchmark Lab)
  formalizes them. This is the largest single remaining credibility gap vs.
  the SPEC.
- **New external signal:** None. Stars/forks unchanged (1 star, 0 forks). No
  inbound issues or PRs from external contributors in this cycle.

## 3. Product outlook

**Human-user view:** The product is still pre-consumer. The closest a human
gets to "using" Rush today is booting the mkosi-built image in QEMU. The
v0.5 closure means the install path is real (not just "boots in CI"), but
there is no desktop, no installer GUI, and no hardware-tuned behavior yet.
v0.6 (hardware-aware optid) and v0.7 (desktop edition) are the two
milestones between the project and a first useful human-facing release.

**AI-agent/contributor view:** Strong. The Dragnet protocol, Builder/Verifier
split, docmap, and `start-work.sh` / `finish-work.sh` lifecycle make this one
of the more agent-friendly repos in its weight class. The v0.6 proposal
(PR #175, `docs/plans/v0.6-hardware-aware-optid-proposal.md`) breaks Phase
A/B/C into in-container Work Packages that an agent can execute without
hardware — only Phase D needs the project owner's hands.

## 4. Reach / visibility — README/landing quality, discoverability, rendered evidence

**README:** Honest and well-structured. The "Evidence Rule" section is a
strong differentiator. After PR #176 merges, the status table will correctly
show v0.5 as complete and v0.6 as in progress.

**Discoverability:** Weak. 1 star, 0 forks, 0 watchers (excluding owner). No
external mentions detected. The project would benefit from:

- A first published benchmark (v0.6 Phase D or v0.8).
- A "Why Rush Linux" landing section that contrasts with PPD/TLP/tuned
  concretely (currently the README describes *what* Rush is, not *why*
  someone should switch).
- A blog post or HN submission timed to the v0.6 release.

**Rendered evidence:** Excellent *internally*; invisible *externally*. The
`release/evidence/` tree is a model of evidence-driven development, but
nothing surfaces it to a casual GitHub visitor. A `release/evidence/README.md`
index that renders the milestone/criterion tree as a single table with links
to transcripts would close this gap.

## 5. Evidence gates (SPEC §6) — LATENCY / ENERGY status

- **LATENCY:** Tooling (`rushbench`) exists; baselines do not. v0.6 Phase D
  is the first time latency transcripts land. Status: **incomplete but on
  plan.**
- **ENERGY:** Same. The energy-window collection logic is implemented; no
  published runs. Status: **incomplete but on plan.**

Both gates are scheduled to close in v0.6 Phase D (mixed-load benchmarks on
two reference machines). The v0.6 proposal explicitly nominates this as the
critical-path bottleneck.

## 6. Verdict

**On-track.**

The project closed its largest internal risk (Dragnet-001) this cycle. The
roadmap from v0.6 to v1.0 is concrete and the implementation plans are at
the right level of detail. The two real risks — hardware availability for
v0.6 Phase D and discoverability for the project overall — are both outside
the codebase and addressable in parallel with code work.

## 7. Course-corrections (1–3)

1. **Nominate the two v0.6 reference machines this week.** This is the
   single action that prevents v0.6 from stalling. The v0.6 proposal
   (`docs/plans/v0.6-hardware-aware-optid-proposal.md` §10) lists the open
   questions; the project owner's answer unblocks Phase D.

2. **Add a `release/evidence/README.md` index.** One file, one table, one
   afternoon. Makes the evidence tree visible to GitHub visitors without
   changing any protocol. Pair with a short "Why Rush Linux" paragraph in
   the main README that contrasts with PPD/TLP/tuned.

3. **Time a "first public benchmark" post to v0.6 Phase D completion.** The
   first published `rushbench` results — even on two machines — are the
   project's strongest possible external signal. Draft the blog post / HN
   submission text in parallel with Phase D so it ships the same day the
   transcripts land.

---

## Next reassessment trigger

v0.6 Phase A merge, or 40 more commits to `main`, whichever comes first.

# v0.6.0-beta.1: Hardware-Aware optid — Implementation Proposal

**Author:** Session Agent
**Date:** 2026-06-26
**Status:** Draft — Awaiting Agreement
**Predecessor:** `docs/plans/v0.5-minimal-installable-system-proposal.md` (closed via PR #174, 2026-06-23)

---

## 0. Context

v0.5.0-beta.1 is now genuinely complete. PR #174 committed the four outstanding
build-host acceptance transcripts (`release/evidence/v0.5.0-beta.1/{c1,c2,c3}/`
plus the existing `c4-server-no-desktop`), `tools/dragnet.py --observe` is green,
and `release/milestones.toml` is the honest source of truth.

The next milestone, **v0.6.0-beta.1 — Hardware-Aware optid**, is the first
milestone whose exit criteria cannot be satisfied from a container. It introduces:

1. A hardware allowlist that gates which kernel knobs `optid` may touch.
2. Compatibility D-Bus shims for `power-profiles-daemon` (PPD) and `GameMode`,
   so existing desktop software drives `optid` without code changes on their side.
3. Foreground-application detection so the optimizer can promote the active
   window's class without requiring an explicit `optctl pin`.
4. A VM-guest workload class — PSI is largely meaningless under virtualization
   and the current classifier misjudges guest workloads.
5. The first *quantitative* criteria: "mixed-load responsiveness improves on two
   machines" and "battery behavior matches or improves mainstream defaults."

This proposal mirrors the 5-phase shape of the v0.5 proposal (Phase A through
Phase E), assigns a Builder/Verifier split per `docs/agent-protocol.md`, and
keeps every change behind `--apply` (default remains dry-run).

---

## 1. Exit Criteria Recap

Per `release/milestones.toml`, the four exit criteria for v0.6.0-beta.1 are:

| # | Criterion | Implication |
|---|-----------|-------------|
| 1 | **Unsupported knobs are skipped with reasons** | `optid` must consult a hardware allowlist before every write. Anything not in the allowlist for the detected platform is skipped and the skip is logged with a concrete reason (unsupported CPU model, missing sysfs path, conflicting daemon, etc.). |
| 2 | **Mixed-load responsiveness improves on two machines** | We need two physical reference machines, a defined mixed-load workload, a baseline (mainstream distro defaults), and `rushbench` transcripts showing `optid --apply` reduces measured latency without regressing throughput. |
| 3 | **Battery behavior matches or improves mainstream defaults** | On at least one battery-equipped reference machine, `rushbench` energy-window transcripts must show `optid --apply` does not increase energy per workload unit vs. the distro default (PPD `balanced`). |
| 4 | **No unsafe write occurs outside allowlisted paths** | A test harness enumerates every sysfs/procfs/devfs path `optid` opens with write intent and asserts each path appears in the allowlist for the host platform. |

---

## 2. Current Infrastructure Assessment

### 2.1 What We Have

| Component | Status | Notes |
|-----------|--------|-------|
| `crates/optid` classifier | Functional | 5-class classifier (idle/light/interactive/latency-critical/throughput) driven by PSI + thermal + power supply + loadavg. Reads every 2s. No notion of host hardware capability yet. |
| `crates/optid` actuator | Functional | Writes EPP, `platform_profile`, `cgroup` slices, PM QoS (`/dev/cpu_dma_latency`, `pci/*/pm_qos_resume_us`). All writes gated by `--apply`; dry-run is default. |
| `config/optid/policy.toml` | Functional | Class → action mapping. Already structured; v0.6 adds a `host` section pointing at the allowlist. |
| `config/optid/hardware-allowlist.toml` | **Does not exist** | New file. Schema to be defined in Phase A. |
| `crates/optctl` D-Bus client | Functional | `status`, `explain`, `mode`, `pin`, `--json`. Already supports the `io.rushlinux.Optid1` interface. |
| `rushbench` harness | Partial | Energy + latency collection logic exists; no published baseline runs. v0.6 needs the first real baseline transcripts. |
| `tools/dragnet.py --observe` | Functional | Evidence-integrity gate. Already green post-PR-#174. |
| `tools/start-work.sh` / `finish-work.sh` | Functional | Session lifecycle. Will be used unchanged. |
| `release/evidence/host-bench/` | Scaffolded | Empty directory; ready for the first baseline runs. |
| Two reference machines | **Not yet nominated** | Phase D blocker. The project owner needs to name the two physical hosts (one desktop, one laptop) and confirm physical access for benchmark runs. |

### 2.2 Key Gaps

1. **No hardware allowlist** — `optid` currently writes to any kernel path it
   knows about. v0.6 requires every writable path to be explicitly allowlisted
   per detected platform.
2. **No platform detection** — We need a stable identity for "this host."
   Candidates: DMI sys vendor+product, CPUID family/model, PCI device IDs for
   GPU. Choice made in Phase A.
3. **No PPD compatibility shim** — GNOME and KDE power sliders speak
   `net.hadess.PowerProfiles`. Without this shim, desktop users who install
   Rush get nothing from their slider.
4. **No GameMode compatibility shim** — Steam games call
   `com.feralinteractive.GameMode.RegisterGame` on launch. Without this shim,
   games don't auto-promote to `latency-critical`.
5. **No foreground detection** — Currently class promotion requires
   `optctl pin <app> <class>`. v0.6 needs automatic promotion of the
   foreground window.
6. **No VM-guest class** — `vm.*` workload class is mentioned in the roadmap
   but has no policy or sensor weighting. PSI under KVM is misleading.
7. **No published baselines** — Without baselines from two machines running
   mainstream defaults, "improves on two machines" is unverifiable.
8. **No conflict-detection** — If `tlp`, `tuned`, or `ppd` are running,
   `optid --apply` will fight them. v0.6 must detect and refuse (or mask).

---

## 3. Proposed Implementation Plan

### Phase A: Hardware Allowlist Foundation (Prerequisite for All Criteria)

**Goal:** Define the allowlist schema, the platform-detection logic, and gate
every existing actuator write behind an allowlist lookup. After Phase A,
`optid --apply` cannot write anywhere the allowlist doesn't explicitly permit
for this host.

#### A1. Define `config/optid/hardware-allowlist.toml` schema

**New file:** `config/optid/hardware-allowlist.toml`

Schema sketch (TOML, matching the project's existing config style):

```toml
# hardware-allowlist.toml — gates every writable kernel path optid touches.
#
# A path is writable on a host iff:
#   1. The host's platform entry matches (by dmi match or cpu match), AND
#   2. The path appears under that platform's allow list, AND
#   3. The path exists on the host at actuation time.
#
# A path that fails any check is skipped with a logged reason.

[platform.rush-vm-default]
description = "Default profile for VM guests (QEMU/KVM, OVMF)"
match.dmi.sys_vendor = "QEMU"
match.dmi.product_name = "Standard PC"
# VM-guest class — see Phase C4
workload_class_override = "vm.guest"

[platform.rush-vm-default.allow]
# EPP — present on Intel CPUs since Sandy Bridge; AMD since Zen 2
"sys/devices/system/cpu/cpu*/cpufreq/energy_performance_preference" = { classes = ["idle", "light", "interactive", "throughput"] }
# platform_profile — Linux 5.11+, X86_ACPI
"sys/firmware/acpi/platform_profile" = { classes = ["idle", "light", "interactive", "latency-critical", "throughput"] }
# PM QoS CPU DMA latency — universal on Linux
"dev/cpu_dma_latency" = { classes = ["latency-critical", "interactive"] }

[platform.rush-vm-default.deny]
# sched_ext is not enabled in the default Arch VM kernel; skip with reason
"sys/fs/sched_ext" = "sched_ext not available on this kernel — EEVDF fallback in effect"
```

#### A2. Implement platform detection in `crates/optid`

**New module:** `crates/optid/src/platform/mod.rs`

Detection strategy (first match wins):
1. Read `/sys/class/dmi/id/sys_vendor` + `/sys/class/dmi/id/product_name`.
   If both match a `[platform.*.match.dmi]` entry, use that platform.
2. Otherwise read CPUID family/model via `/proc/cpuinfo` and match against
   `[platform.*.match.cpu]` entries.
3. Otherwise fall back to `[platform.rush-vm-default]` if DMI says QEMU,
   else `[platform.unknown]` (deny everything except dry-run paths).

`[platform.unknown]` is defined as: zero allow entries. This means
`optid --apply` on an unknown host is a no-op with a logged reason per skipped
path. This is the safe default.

#### A3. Gate every actuator write behind the allowlist

**Modify:** `crates/optid/src/actuator/*.rs`

Every `write_sysfs(path, value)` call site must first call
`allowlist.check(path, class)`. If denied, the actuator logs:

```
[SKIP] path=/sys/firmware/acpi/platform_profile class=latency-critical reason="not in allowlist for platform=rush-vm-default"
```

…and continues. No error, no panic. The classifier's decision is preserved in
the log; only the actuation is skipped.

This is the largest single change in v0.6 but it is mechanical: every existing
write site gets a guard.

#### A4. Add `optctl explain` enrichment

**Modify:** `crates/optctl/src/explain.rs`

`optctl explain` currently reports the last decision. Extend it to also report
which paths were skipped and why:

```
$ optctl explain
Decision at 2026-06-26T14:23:01Z
  class: latency-critical
  sensor: psi.cpu.avg10=42.1 (>threshold=30)
  applied:
    /dev/cpu_dma_latency = 0
  skipped:
    /sys/firmware/acpi/platform_profile — not in allowlist for platform=rush-vm-default
    /sys/devices/system/cpu/cpu0/cpufreq/energy_performance_preference — sched_ext fallback in effect
```

This satisfies Criterion 1: "skipped with reasons."

#### A5. Test

**New file:** `crates/optid/tests/allowlist.rs`

Tests:
- Empty allowlist → all writes skipped, each with a reason.
- Allowlist with one path → that path is written, all others skipped.
- DMI match resolves to correct platform.
- Unknown platform → all writes skipped, default to dry-run.
- Allowlist entry that references a non-existent sysfs path → skipped with
  reason "path not present".

All tests are unit tests; they run in-container. Phase A can be verified by a
Verifier agent without any hardware.

**Acceptance:** `cargo test -p optid allowlist` green; `optctl explain` on a
test fixture shows skipped-with-reason output.

---

### Phase B: PPD and GameMode Compatibility Shims (Criterion 1 partial, enabler for desktop editions in v0.7)

**Goal:** Implement `net.hadess.PowerProfiles` and `com.feralinteractive.GameMode`
D-Bus interfaces on the `optid` D-Bus name, so existing desktop software drives
`optid` mode changes without forking PPD or GameMode.

#### B1. PPD shim (`net.hadess.PowerProfiles`)

**New module:** `crates/optid/src/shim/ppd.rs`

Implements:
- `org.freedesktop.DBus.Properties.Get("net.hadess.PowerProfiles", "ActiveProfile")`
- `Set` for the same.
- `org.freedesktop.DBus.Properties.Get("net.hadess.PowerProfiles", "Profiles")`
  returning `[{Profile:"power-saver"}, {Profile:"balanced"}, {Profile:"performance"}]`.
- `HoldProfile` / `ReleaseProfile` — PPD's API for transient claims.

Mapping (configurable via `policy.toml`):

| PPD profile | optid mode |
|-------------|------------|
| `power-saver` | `--pin class=idle` |
| `balanced` | (clear all pins; revert to classifier) |
| `performance` | `--pin class=throughput` |

GNOME Settings → Power slider now drives `optid` directly. KDE's `powerdevil`
speaks the same interface.

#### B2. GameMode shim (`com.feralinteractive.GameMode`)

**New module:** `crates/optid/src/shim/gamemode.rs`

Implements:
- `RegisterGame(pid)`
- `UnregisterGame(pid)`
- `QueryStatus()`
- `QueryStatusClient(pid)`

On `RegisterGame(pid)`, `optid` issues an implicit `pin <pid> latency-critical`
with a TTL of 30 minutes (configurable). On `UnregisterGame(pid)`, the pin is
released. Steam, Lutris, and Heroic already speak this interface; no client
changes are needed.

#### B3. Conflict detection

**New module:** `crates/optid/src/shim/conflict.rs`

On startup, `optid` checks for running daemons that conflict with the shims:
- `tlp.service` → log error, refuse `--apply` until masked.
- `tuned.service` → same.
- `power-profiles-daemon.service` → log error: "PPD is running. Either stop
  PPD (recommended, optid provides the PPD interface) or stop optid's PPD shim
  via `--no-shim=ppd`."

This protects Criterion 4: if two daemons fight over `/sys/firmware/acpi/platform_profile`,
both can produce inconsistent writes — exactly what the allowlist is meant to
prevent. The conflict check is itself part of the allowlist enforcement.

#### B4. Test

**New file:** `crates/optid/tests/shim_ppd.rs`, `crates/optid/tests/shim_gamemode.rs`

Use `zbus` test bindings to validate the D-Bus interfaces respond correctly
without a real session bus. All in-container.

**Acceptance:** `cargo test -p optid shim_` green. Manual: `gdbus call --session
--dest io.rushlinux.Optid1 --object-path /net/hadess/PowerProfiles
--method org.freedesktop.DBus.Properties.Get net.hadess.PowerProfiles ActiveProfile`
returns the current mode.

---

### Phase C: Foreground Detection and VM-Guest Class (Criterion 1 cont., enables Criterion 2)

**Goal:** Replace the explicit `optctl pin` model with automatic foreground
promotion for desktop sessions, and add a `vm.guest` workload class so the
classifier doesn't misjudge KVM guests.

#### C1. Foreground detection via `logind` + `wayland`

**New module:** `crates/optid/src/foreground/mod.rs`

Strategy:
1. Subscribe to `org.freedesktop.login1` `SessionNew` / `SessionRemoved`.
2. For each new session, check `Active` and `Class` (= "user").
3. If the session is `Type=wayland` or `Type=x11`, subscribe to the compositor's
   focus signal:
   - Wayland: `org.gnome.Mutter.IdleMonitor` (GNOME) / `org.kde.KWin` (KDE) —
     or fall back to `wlr-foreign-toplevel-management` (wlroots/sway/Hyprland).
   - X11: `_NET_ACTIVE_WINDOW` root window property via `xcb`.
4. On focus change, issue `pin <pid-of-focused-window> <class>` where `class`
   comes from `policy.toml` `[foreground.*]` rules (e.g., browser=interactive,
   IDE=throughput, game=latency-critical).

For v0.6 the foreground rules are minimal — just "game window → latency-critical,
everything else → no override." Elaboration deferred to v0.7.

#### C2. `vm.guest` workload class

**Modify:** `crates/optid/src/classifier/mod.rs`

Add a sixth class `vm.guest`. The classifier detects VMs via DMI (`sys_vendor ==
QEMU|KVM|VMware|Xen|Microsoft`) and, when in a VM, uses a different sensor
weighting:
- PSI avg10 is multiplied by 0.5 (the hypervisor's scheduler distorts it).
- `loadavg` is the primary signal.
- `latency-critical` is downgraded to `interactive` (we can't enforce real
  latency floors in a guest anyway — PM QoS to the host CPU doesn't propagate).

The `vm.guest` class is *not* user-selectable via `optctl pin` — it's forced by
the platform detector. This prevents a user from pinning a guest to
`latency-critical` and expecting host-level behavior.

#### C3. Test

Foreground detection is hard to unit-test (requires a compositor). For v0.6,
scope the test to: the `foreground` module compiles, exposes a `subscribe()`
function that returns a stream of `(pid, class)` events, and the `policy.toml`
parser accepts `[foreground.*]` rules. Real-compositor testing is deferred to
v0.7 when we have a desktop edition.

VM-guest class is fully unit-testable: feed a fixture where DMI says QEMU and
verify the classifier selects `vm.guest`.

**Acceptance:** `cargo test -p optid vm_guest` green. Foreground detection
compiles and is wired into `optid` behind a `--foreground=auto|off` flag
(default `off` for v0.6; `auto` becomes default in v0.7).

---

### Phase D: Reference Machines, Baselines, and Benchmark Transcripts (Criteria 2 & 3)

**Goal:** Produce the first published `rushbench` transcripts: baselines from
two physical machines running mainstream defaults, then `optid --apply` runs
on the same two machines, and a comparison.

This is the heaviest phase and the one with the most external dependencies.
It cannot be done by an agent alone.

#### D1. Nominate two reference machines

**Decision required from project owner before this phase starts:**

| Slot | Suggested profile | Why |
|------|-------------------|-----|
| Desktop | Modern Intel or AMD, 6+ cores, discrete GPU, on AC always | Tests throughput class, latency-critical (gaming), no battery criterion |
| Laptop | Modern Intel laptop (U-class), iGPU, battery, AC + battery modes | Tests battery criterion (Criterion 3), idle/light classes, platform_profile on real hardware |

The project owner must confirm: (a) which specific machines, (b) who has
physical access, (c) which distro is the baseline (suggested: Ubuntu 24.04
LTS with default PPD `balanced` profile — the most common mainstream default).

Document the choice in `docs/strategy/reference-hardware.md` (new file).

#### D2. Define the mixed-load workload

**New file:** `docs/strategy/mixed-load-workload.md`

The workload must exercise all four active classes in a single run so the
optimizer's class transitions are observable. Suggested mix:

1. **Idle** (60s): nothing.
2. **Interactive** (60s): `firefox` rendering a JavaScript benchmark.
3. **Throughput** (60s): `ninja -C build` on a medium C++ project.
4. **Latency-critical** (60s): `glmark2` fullscreen at 60 FPS target.
5. **Idle** (30s): cool-down.

Repeat ×5 for stability. Total: ~30 minutes per run.

`rushbench` already supports structured energy windows and N-sample collection;
this phase wires the workload definition into `rushbench` as a named preset
(`rushbench run preset=mixed-load-001`).

#### D3. Baseline runs

For each reference machine:
1. Install the baseline distro (Ubuntu 24.04 LTS, default install, default PPD).
2. Run `rushbench run preset=mixed-load-001 --tag=baseline-ubuntu-2404-<hostname>`.
3. Commit `meta.txt` + `transcript.log` + `results.csv` to
   `release/evidence/host-bench/<date>-<hostname>/baseline/`.

These baselines are not optid-specific; they are the comparison point for the
v0.6 release. They will also serve v0.8 (Benchmark Lab).

#### D4. optid runs

For each reference machine:
1. Install Rush Linux v0.6.0-beta.1 (server edition is fine for the desktop
   machine; we don't need a desktop for these tests — the workload is launched
   from a TTY).
2. Boot with `optid` in dry-run mode. Confirm `optctl status` is green.
3. Enable `optid --apply` via `optctl mode apply`.
4. Re-run `rushbench run preset=mixed-load-001 --tag=optid-0.6.0-beta.1-<hostname>`.
5. Commit transcripts to `release/evidence/host-bench/<date>-<hostname>/optid/`.

#### D5. Comparison and verdict

For each machine:
1. Compute the delta between baseline and optid runs:
   - Latency: PSI avg10 mean and p99 per phase.
   - Energy: joules per phase, joules per workload unit.
2. Write `release/evidence/host-bench/<date>-<hostname>/VERDICT.md`:
   - "Criterion 2 (mixed-load responsiveness improves): PASS / FAIL — <evidence>"
   - "Criterion 3 (battery behavior matches or improves): PASS / FAIL / N/A — <evidence>"
3. Update `release/milestones.toml` criteria_status rows for v0.6.

**Acceptance:** Both machines have committed transcripts in
`release/evidence/host-bench/`. `tools/dragnet.py --observe` is green. The
verdict text in each `VERDICT.md` is the literal source for the
`milestones.toml` `note` field.

---

### Phase E: Documentation, Conflict-Masking Policy, and Lifecycle

#### E1. Documentation updates (per docmap)

- `docs/IMPLEMENTATION_STATUS.md` — Add hardware-allowlist, PPD/GameMode shims,
  foreground detection, vm.guest class.
- `docs/AI_CONTINUATION.md` — Update "Next Task" to point at v0.7 (Editions).
- `docs/packaging-and-builds.md` — Document the new `config/optid/hardware-allowlist.toml`
  and how to add a platform entry.
- `docs/boot-and-updates.md` — Add conflict-masking behavior to the "Running
  optid" section.
- `docs/strategy/reference-hardware.md` — New file (per D1).
- `docs/strategy/mixed-load-workload.md` — New file (per D2).
- `ROADMAP.md` — Mark v0.6 complete.
- `RELEASES.md` — Add v0.6 row.
- `docs/docmap.toml` — New entries for new files; bump `last_verified` for
  every modified file.

#### E2. Conflict-masking policy

**New section in `docs/agent-protocol.md`:**

When `optid` detects a conflicting daemon at startup:
1. Log an error.
2. If running in `--apply` mode, refuse to apply. Stay in dry-run.
3. Suggest the masking command: `systemctl mask --now tlp.service tuned.service
   power-profiles-daemon.service`.
4. Do not auto-mask. Auto-masking is a v0.7 consideration (and may never land —
   it's user-policy territory).

#### E3. Validation suite

```bash
bash tools/start-work.sh "v0.6.0-beta.1 milestone completion"
cargo test --workspace
python3 tools/validate-doc-sync.py --max-age 90
python3 tools/dragnet.py --observe   # must be green
bash tools/finish-work.sh "milestone(v0.6.0-beta.1): Hardware-Aware optid"
```

#### E4. Version bump

Single PR (mirror PR #157's shape): bump `VERSION` → `0.7.0-beta.1` across all
~14 files; this PR opens the v0.7 milestone and closes v0.6.

---

## 4. Implementation Order and Dependencies

```
Phase A (Allowlist Foundation)            ← In-container; first Builder WP
  A1 (schema) → A2 (detection) → A3 (gating) → A4 (explain) → A5 (tests)
  └─ A5 satisfies Criterion 1 (partial — needs C2 + B3 to be complete)

Phase B (PPD + GameMode shims)            ← In-container; second Builder WP
  B1 (PPD) → B2 (GameMode) → B3 (conflict) → B4 (tests)
  └─ Parallel to A once A1 lands

Phase C (Foreground + VM-guest class)     ← In-container; third Builder WP
  C1 (foreground) → C2 (vm.guest) → C3 (tests)
  └─ C2 contributes to Criterion 1 (vm path skips)
  └─ C1 is feature-flagged off by default in v0.6

Phase D (Reference machines + baselines)  ← Hardware; project owner + Verifier
  D1 (nominate) → D2 (workload) → D3 (baselines) → D4 (optid) → D5 (verdict)
  └─ D5 satisfies Criteria 2 & 3
  └─ Cannot start until A+B+C merged

Phase E (Docs + lifecycle)                ← In-container; final Builder WP
  E1 → E2 → E3 → E4
  └─ E3 gates the version bump
```

**Critical path:** A1 → A2 → A3 → A4 → A5 → (B,C parallel) → D1 → D2 → D3 → D4 → D5 → E3 → E4

**Parallelizable work:** B can start once A1 lands (schema is shared). C1
(foreground) is fully independent and can proceed in parallel with A. E1 can
proceed incrementally from A onward.

---

## 5. New Files to Create

| File | Purpose | Phase |
|------|---------|-------|
| `config/optid/hardware-allowlist.toml` | Allowlist of writable kernel paths per platform | A |
| `crates/optid/src/platform/mod.rs` | Platform detection (DMI/CPUID) | A |
| `crates/optid/src/shim/ppd.rs` | PPD D-Bus compatibility shim | B |
| `crates/optid/src/shim/gamemode.rs` | GameMode D-Bus compatibility shim | B |
| `crates/optid/src/shim/conflict.rs` | Conflict detection for tlp/tuned/ppd | B |
| `crates/optid/src/foreground/mod.rs` | Foreground application detection | C |
| `crates/optid/tests/allowlist.rs` | Allowlist unit tests | A |
| `crates/optid/tests/shim_ppd.rs` | PPD shim tests | B |
| `crates/optid/tests/shim_gamemode.rs` | GameMode shim tests | B |
| `docs/strategy/reference-hardware.md` | Reference machine declarations | D |
| `docs/strategy/mixed-load-workload.md` | Mixed-load benchmark definition | D |
| `release/evidence/host-bench/<date>-<hostname>/baseline/` | Baseline transcripts | D |
| `release/evidence/host-bench/<date>-<hostname>/optid/` | optid transcripts | D |
| `release/evidence/host-bench/<date>-<hostname>/VERDICT.md` | Per-machine verdict | D |

## 6. Existing Files to Modify

| File | Changes | Phase |
|------|---------|-------|
| `crates/optid/src/actuator/*.rs` | Gate every write behind allowlist check | A |
| `crates/optid/src/classifier/mod.rs` | Add `vm.guest` class, VM detection | C |
| `crates/optid/src/lib.rs` | Wire platform/shim/foreground modules | A,B,C |
| `crates/optctl/src/explain.rs` | Show skipped paths with reasons | A |
| `config/optid/policy.toml` | Add `[foreground.*]` and `[platform.*]` sections | A,C |
| `docs/IMPLEMENTATION_STATUS.md` | Track new implemented items | E |
| `docs/AI_CONTINUATION.md` | Update next task → v0.7 | E |
| `docs/packaging-and-builds.md` | Document allowlist schema | E |
| `docs/boot-and-updates.md` | Document conflict-masking | E |
| `docs/agent-protocol.md` | Add conflict-masking policy section | E |
| `ROADMAP.md` | Mark v0.6 complete | E |
| `RELEASES.md` | Add v0.6 row | E |
| `release/milestones.toml` | Add `criteria_status` rows for v0.6 | D,E |
| `docs/docmap.toml` | New entries; bump `last_verified` | E |

---

## 7. Risk Assessment

| Risk | Likelihood | Mitigation |
|------|------------|------------|
| Reference machines not nominated → Phase D stalls | High | Phase D1 is a hard gate; surface it to the project owner in the PR description for Phase A so they have weeks of lead time |
| `systemd-logind` D-Bus subscription is unstable across compositors | Medium | Ship foreground detection as `--foreground=off` default in v0.6; flip to `auto` in v0.7 after broader testing |
| PPD API drift between GNOME/KDE versions | Low | Pin to the documented `net.hadess.PowerProfiles` API (stable since 2021); add a shim test that exercises the interface against a mock |
| `vm.guest` class masks real performance issues in CI | Medium | The classifier logs which class is active; CI runs that show `vm.guest` should be flagged for human review |
| `rushbench` baselines vary more than the optid delta | High | Use 5-sample runs (already supported); compute confidence intervals; report PASS only if the delta exceeds the confidence interval |
| Battery criterion is N/A on the desktop machine | Medium | `VERDICT.md` explicitly permits N/A on non-battery hosts; Criterion 3 then rests on the laptop alone |
| Allowlist schema changes after A1 lands, breaking B and C | Medium | Land A1 in its own PR; require B and C PRs to rebase on A1 before merge |

---

## 8. What We Are NOT Doing (Explicitly Out of Scope for v0.6)

- **No desktop edition build** — That is v0.7. The PPD/GameMode shims land in
  v0.6 but they are tested against a headless server edition; full desktop
  testing happens in v0.7.
- **No laptop-specific power policy** — Laptop edition lands in v0.7. The
  battery criterion in v0.6 is satisfied by running the server edition on the
  laptop; the optimizer's behavior is the same.
- **No realtime audio edition** — v0.7.
- **No published benchmark report** — v0.8 (Benchmark Lab). v0.6 only produces
  the transcripts; v0.8 turns them into a published artifact.
- **No automatic conflict masking** — `optid` reports conflicts; the user
  decides whether to mask. Auto-masking is policy territory.
- **No Secure Boot enrollment** — Testing keys only; real enrollment is v0.9.
- **No `optctl pin --ttl`** — Pins are still explicit-release. TTL-based
  release is a v0.7 consideration once GameMode integration has shipped.
- **No new sched_ext scheduler** — We continue to use EEVDF as the verified
  fallback. A Rush-specific `sched_ext` program is a v0.8+ research item.

---

## 9. Builder/Verifier Split

Per `docs/agent-protocol.md`:

| Phase | Builder | Verifier | Evidence |
|-------|---------|----------|----------|
| A | One Builder session, one PR (`feat(optid): hardware allowlist foundation`) | Separate Verifier session, cold checkout, runs `cargo test -p optid allowlist` | `VERIFICATION.md` with literal transcript |
| B | One Builder session, one PR (`feat(optid): PPD/GameMode shims`) | Verifier runs `cargo test -p optid shim_` and a manual `gdbus call` smoke test | Same |
| C | One Builder session, one PR (`feat(optid): foreground + vm.guest class`) | Verifier runs `cargo test -p optid vm_guest` and confirms `--foreground=off` is default | Same |
| D | Project owner (manual) | Verifier agent reviews committed transcripts and checks `dragnet.py --observe` is green | `VERDICT.md` per machine |
| E | One Builder session, one PR (`milestone(v0.6.0-beta.1): close milestone, bump to v0.7`) | Verifier runs full validation suite | Same |

No Builder certifies its own work. Every PR carries a `VERIFICATION.md`
produced by a separate Verifier session.

---

## 10. Open Questions for the Project Owner

1. **Which two reference machines?** Suggested: one Intel desktop (i5-13400 or
   similar), one Intel laptop (ThinkPad X1 or similar). Confirm before Phase A
   merges so D1 isn't a blocker.
2. **Which baseline distro?** Suggested: Ubuntu 24.04 LTS with default PPD
   `balanced`. Alternative: Fedora 40 with default PPD. Pick one and stick
   with it for v0.6 and v0.8.
3. **Is `--foreground=off` acceptable as the v0.6 default?** The alternative
   is `auto`, which is more impressive but riskier.
4. **Conflict masking: should `optid` refuse to start, or start in dry-run?**
   Current proposal: dry-run. Alternative: hard refusal.
5. **PPD and GameMode shims: should they be on by default, or behind
   `--shim=ppd,gamemode`?** Current proposal: on by default, behind
   `--no-shim=...` for opt-out.

Answers can be inline comments on the PR that lands this proposal.

# Independent cold-verification receipt for S3D.
# Previous receipts and builder recertifications remain preserved in Git history.

schema_version = 1
package = "S3D"
implementation_pr = 388
verification_pr = 447
verified_commit = "b673c0e185639c556a69e88e806ed11946f526d1"
verifier = "Independent ChatGPT GitHub verifier; GitHub Actions ubuntu-24.04"
result = "pass"
workflow_run = 33332810449
workflow_job = 99314354620

commands = [
  "PR Gate run 33332810449 job 99314354620: cargo fmt --all -- --check -> PASS",
  "PR Gate run 33332810449 job 99314354620: cargo test --workspace -> PASS; optid main 548 passed, optid-recover 14 passed, recovery_cli 4 passed, s3d_systemd 2 passed",
  "PR Gate run 33332810449 job 99314354620: cargo test -p optid --features test-simulation --test i2_simulation_evidence_cli -> PASS; 6 passed including supervised restart recovery",
  "PR Gate run 33332810449 job 99314354620: cargo clippy --workspace --all-targets -- -D warnings -> PASS",
  "PR Gate run 33332810449 job 99314354620: cargo check --workspace --all-targets --all-features -> PASS",
  "PR Gate run 33332810449 job 99314354620: python3 tools/validate-optid-packages.py --base 99069058ede92065083c39e408b7803b6aca0f8c -> PASS",
]

runtime_proofs = [
  "All mapped S3D recovery tests passed: intended-value restore, prepared-record idempotence, failure retention, external-drift handback, identity-reuse refusal, and repeated recovery idempotence.",
  "Production recovery CLI tests proved recovery occurs before successful exit, unresolved records are retained, removed or replaced targets are not touched, and the recovery binary has no policy or async surface.",
  "Watchdog tests emitted readiness and heartbeat only after a complete healthy cycle and withheld heartbeat on journal failure; systemd tests ordered recovery before apply and prevented an automatic actuation restart loop after failed recovery.",
  "The exact verified commit differs from main only by the temporary rush_collect CI-selection test and changes no S3D production, test, service, packaging, or declared proof path.",
]

unresolved = []

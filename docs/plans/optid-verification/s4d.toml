# Independent cold-verification receipt for S4D.
# Previous receipts and builder recertifications remain preserved in Git history.

schema_version = 1
package = "S4D"
implementation_pr = 390
verification_pr = 447
verified_commit = "b673c0e185639c556a69e88e806ed11946f526d1"
verifier = "Independent ChatGPT GitHub verifier; GitHub Actions ubuntu-24.04"
result = "pass"
workflow_run = 33332810449
workflow_job = 99314354620

commands = [
  "PR Gate run 33332810449 job 99314354620: cargo fmt --all -- --check -> PASS",
  "PR Gate run 33332810449 job 99314354620: cargo test --workspace -> PASS; optid main 548 passed and s4d_systemd 3 passed",
  "PR Gate run 33332810449 job 99314354620: cargo test -p optid --features test-simulation --test i2_simulation_evidence_cli -> PASS; 6 passed including hot-removal handback and one clean supervised restart",
  "PR Gate run 33332810449 job 99314354620: cargo clippy --workspace --all-targets -- -D warnings -> PASS",
  "PR Gate run 33332810449 job 99314354620: cargo check --workspace --all-targets --all-features -> PASS",
  "PR Gate run 33332810449 job 99314354620: python3 tools/validate-optid-packages.py --base 99069058ede92065083c39e408b7803b6aca0f8c -> PASS",
]

runtime_proofs = [
  "All eleven mapped S4D acceptance tests passed: typed operation mismatch, permission tightening with pre-opened descriptors, symlink replacement, stale identity, removed device, CLOEXEC, topology debounce, cold rebuild, and all three systemd lifecycle checks.",
  "Capability-table tests failed closed for stale, replaced, removed, or operation-mismatched targets while preserving already-opened authorized descriptor behavior.",
  "Systemd tests proved startup sealing occurs before worker or D-Bus input, topology rebuild hands back before status 75, and restart happens only through the supervised recovery graph; simulated hot removal completed handback before one clean restart.",
  "The exact verified commit differs from main only by the temporary rush_collect CI-selection test and changes no S4D production, test, service, policy, packaging, or declared proof path.",
]

unresolved = []

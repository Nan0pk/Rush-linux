# Independent cold-verification receipt for:
# Version observations, decisions, and action outcomes
#
# Historical provenance (preserved):
# - First post-#369 attempt (2026-08-01, prior verifier session): recorded
#   result = "blocked" against verified_commit
#   107a4330e2d0505a6b48b35c7d4148482a426f3d because the previous sandbox
#   could not execute the Rust toolchain (no cargo/rustc; static.rust-lang.org
#   and crates.io unreachable; rustup toolchain download failed). No runtime
#   proof existed. Retained as historical provenance only.
# - Post-#369 executable receipt (Arena.ai verifier, PR #370, 2026-08-02):
#   verified PR #369 at commit 107a4330e2d0505a6b48b35c7d4148482a426f3d
#   (merge commit of PR #369, HEAD of main at the time). All six mapped F3
#   in-crate acceptance tests and all three mapped F3 optctl status_cli
#   integration tests passed individually with --exact. F1 was independently
#   verified first (see f1.toml). Invalidated because PR #371 modified the
#   shared production entry point crates/optid/src/main.rs AND extended the
#   production control-cycle restore path after that verification, truthfully
#   demoting F3 to merged_incomplete until a fresh cold verifier re-ran the
#   mapped F3 tests and versioned production envelope against a commit
#   containing the F4 cutover. The full prior receipt is preserved below in
#   [historical_receipt.post_369_arena_370].
#
# This receipt records the executable independent cold-verification of the F3
# production surface at implementation commit
# 52919c2ac1582e56737d568a576899e10ae43d15 (HEAD of
# fix/f4-complete-reconciliation, PR #371). The F4 cutover modified
# crates/optid/src/main.rs to construct and consume the F4 Reconciler as the
# sole production reconciliation authority, and extended the production
# control-cycle restore path to drive restoration through the F4 Reconciler
# and include F4 restore outcomes in the versioned F3 envelope. This verifier
# session re-executed every mapped F3 in-crate acceptance test, every mapped
# F3 optctl status_cli integration test, and the full status_cli binary
# against the exact F4 implementation commit and confirmed F3 still passes.
# F3's F1 dependency was independently re-verified `pass` first (see f1.toml),
# so the package-contract dependency gate is satisfied. F3 is re-certified
# `pass` by this receipt.
#
# Independence boundary: this verifier is separate from the implementation
# builder of PR #371 and separate from the prior Arena.ai verifier session
# (PR #370). No production code, test, assertion, workflow, architecture,
# policy, configuration, or packaging file was modified during verification.
#
# Environmental caveat (push only): the verifier sandbox had no GitHub write
# credentials. The verification itself was complete and genuine — every
# command recorded below was actually executed against the exact detached
# implementation commit and observed to pass. The evidence commit was prepared
# locally but could not be pushed from this sandbox.

schema_version = 1
package = "F3"
implementation_pr = 369
# Commit under verification for this run: HEAD of fix/f4-complete-reconciliation
# (PR #371), which contains the F4 cutover of the shared main.rs entry point
# and the extended production control-cycle restore path. All nine mapped F3
# acceptance tests (six in-crate + three optctl status_cli) were executed
# against this commit. `git merge-base --is-ancestor
# 107a4330e2d0505a6b48b35c7d4148482a426f3d HEAD` returns 0 (the prior
# verified commit is an ancestor), confirming the F3 production surface added
# in PR #369 is still present and now exercises the F4-cutover restore path.
verified_commit = "52919c2ac1582e56737d568a576899e10ae43d15"
verifier = "Independent cold-verification agent session (Z.ai sandbox, separate from the implementation builder of PR #371 and from the prior Arena.ai verifier session that certified F3 at 107a4330e2d0505a6b48b35c7d4148482a426f3d on PR #370); rustc 1.97.1 (8bab26f4f 2026-07-14), cargo 1.97.1 (c980f4866 2026-06-30), pwsh 7.4.7, shellcheck 0.10.0, actionlint 1.7.7, Python 3.12.13, git 2.47.3; executed 2026-08-02 in a detached worktree at the exact PR head with a clean cargo state"
result = "pass"

# Exact commands this verifier executed, with their observed outcomes.
commands = [
  "git fetch origin --prune -> EXECUTED, fetched origin/fix/f4-complete-reconciliation and origin/main",
  "git worktree add --detach ../Rush-linux-f4-verify 52919c2ac1582e56737d568a576899e10ae43d15 -> EXECUTED, HEAD=52919c2ac1582e56737d568a576899e10ae43d15, working tree clean",
  "git rev-parse HEAD -> EXECUTED, 52919c2ac1582e56737d568a576899e10ae43d15",
  "git status --short -> EXECUTED, empty (clean detached worktree)",
  "git merge-base --is-ancestor 107a4330e2d0505a6b48b35c7d4148482a426f3d HEAD -> EXECUTED, exit 0 (PR #369 merge commit IS ancestor of the F4 implementation commit, so the F3 production surface is still present)",
  "rustc --version --verbose -> EXECUTED, rustc 1.97.1 (8bab26f4f 2026-07-14)",
  "cargo --version -> EXECUTED, cargo 1.97.1 (c980f4866 2026-06-30)",
  "python3 --version -> EXECUTED, Python 3.12.13",
  "rustup component add rustfmt clippy -> EXECUTED, installed",
  "cargo fmt --all -- --check -> EXECUTED, exit 0 (clean)",
  "cargo check --workspace --all-targets --all-features -> EXECUTED, exit 0 (Finished dev profile in 36.08s)",
  "cargo clippy --workspace --all-targets -- -D warnings -> EXECUTED, exit 0 (Finished dev profile in 7.49s, no warnings)",
  "cargo test --workspace -> EXECUTED, exit 0; optid lib 6 passed, optid main 421 passed, optctl main 9 passed, optctl status_cli 3 passed, rushbench 13 passed, testos lib 51 passed, testos-ingest 16 passed; total 519 passed, 0 failed, 0 ignored",
  "cargo test --bin optid tests::f3_production_surface::f3_production_daemon_run_writes_one_correlated_versioned_cycle -- --exact -> EXECUTED, 1 passed; 0 failed; 420 filtered out (this test enters through crate::run(args) and now transitively exercises the F4 Reconciler via main.rs:203/311/336/369; restore_outcomes from reconciler.reconcile() flow into ControlCycleEnvelope::build at main.rs:370-377)",
  "cargo test --bin optid tests::f3_outcome_matrix::f3_gate_matrix_distinguishes_dry_run_observe_off_and_boot_disarm -- --exact -> EXECUTED, 1 passed; 0 failed; 420 filtered out",
  "cargo test --bin optid tests::f3_outcome_matrix::f3_actuator_matrix_reports_missing_unsupported_contract_and_allowlist_denials -- --exact -> EXECUTED, 1 passed; 0 failed; 420 filtered out",
  "cargo test --bin optid tests::f3_outcome_matrix::f3_actuator_matrix_reports_success_failure_readback_drift_and_redundancy -- --exact -> EXECUTED, 1 passed; 0 failed; 420 filtered out",
  "cargo test --bin optid tests::f3_outcome_matrix::f3_restore_matrix_reports_actual_success_failure_malformed_and_not_applicable -- --exact -> EXECUTED, 1 passed; 0 failed; 420 filtered out",
  "cargo test --bin optid tests::f3_outcome_matrix::f3_observation_matrix_distinguishes_malformed_and_permission_denied_reads -- --exact -> EXECUTED, 1 passed; 0 failed; 420 filtered out",
  "cargo test --test status_cli f3_optctl_status_json_passthrough_uses_state_fallback_and_is_read_only -- --exact -> EXECUTED, 1 passed; 0 failed; 2 filtered out",
  "cargo test --test status_cli f3_optctl_status_json_rejects_malformed_and_missing_machine_state -- --exact -> EXECUTED, 1 passed; 0 failed; 2 filtered out",
  "cargo test --test status_cli f3_optctl_human_status_remains_backward_compatible_and_is_read_only -- --exact -> EXECUTED, 1 passed; 0 failed; 2 filtered out",
  "cargo test --test status_cli -> EXECUTED, 3 passed; 0 failed; 0 ignored; 0 filtered out; finishing in 0.01s",
  "python3 tools/validate-current-work.py -> EXECUTED, OK",
  "python3 tools/validate-optid-packages.py -> EXECUTED, PASS",
  "python3 tools/validate-optid-packages.py --base bc6d974857a69da88795db8feb6d3f8cb75b5e21 -> EXECUTED, PASS",
  "python3 tools/render-frontpage.py --check -> EXECUTED, OK",
  "bash tools/finish-work.sh --dry-run -> EXECUTED, 'Nothing to finish: the working tree is clean.'",
  "bash tools/checks.sh --ci --changed-base bc6d974857a69da88795db8feb6d3f8cb75b5e21 -> EXECUTED, exit 0; pwsh 7.4.7, shellcheck 0.10.0, actionlint 1.7.7 installed; all sections passed; final line 'PASS: selected Rush checks passed.'",
]

# Runtime proofs entered through the real production surfaces named by the
# F3 ledger entry (crates/optid/src/main.rs, envelope.rs, dbus.rs,
# crates/optctl/src/main.rs, packaging/dbus/io.rushlinux.Optid.xml). All
# six mapped in-crate F3 acceptance tests and all three mapped optctl
# status_cli integration tests were executed successfully against the
# verified commit.
runtime_proofs = [
  "Gate distinctions: f3_gate_matrix_distinguishes_dry_run_observe_off_and_boot_disarm (tests.rs:386, mod f3_outcome_matrix) drives the real production Actuator (Actuator::new_with_kernel) through injected MemoryKernel/FaultKernel I/O and production Action variants, covering dry-run, observe, off, and boot-disarm gate outcomes as typed GateStage/GateReasonCode records. EXECUTED: 1 passed; 0 failed.",
  "Denial distinctions: f3_actuator_matrix_reports_missing_unsupported_contract_and_allowlist_denials (tests.rs:448) confirms the actuator matrix reports missing, unsupported, contract, and allowlist denial outcomes distinctly through the real production Actuator path. EXECUTED: 1 passed; 0 failed.",
  "Apply and readback outcomes: f3_actuator_matrix_reports_success_failure_readback_drift_and_redundancy (tests.rs:541) confirms success, failure, readback-drift, and redundancy outcomes are reported distinctly. EXECUTED: 1 passed; 0 failed.",
  "Restoration outcomes (legacy revert_key_outcome path): f3_restore_matrix_reports_actual_success_failure_malformed_and_not_applicable (tests.rs:633) drives Actuator::revert_key_outcome directly (a legacy compatibility surface now link-checked only from production main.rs; see link_retired_restore_compatibility_surface at main.rs:88-101) and confirms restore success, failure, malformed, and not-applicable outcomes are reported distinctly. The F4 production restore path is exercised separately by the F4 acceptance tests (see f4.toml). EXECUTED: 1 passed; 0 failed.",
  "Observation failures: f3_observation_matrix_distinguishes_malformed_and_permission_denied_reads (tests.rs:727) confirms malformed and permission-denied observation reads are distinguished through the real production Snapshot path. EXECUTED: 1 passed; 0 failed.",
  "One correlated, versioned production cycle (post-F4 cutover): f3_production_daemon_run_writes_one_correlated_versioned_cycle (tests.rs:148, mod f3_production_surface) enters through the real daemon crate::run(args) under with_real_kernel_override with a deterministic injected clock and confirms: status.json parses as ControlCycleEnvelope with ENVELOPE_SCHEMA_VERSION (2), cycle_timestamp equals the injected clock, off/observe/actuate domains appear with correct selected_mode, observe retains the would-be action with GateReasonCode::DomainObserve and no write_attempted targets, exactly one control-cycles.jsonl line matching status.json byte-for-byte, and the same correlation_id in human status and decisions.log. Post-F4, the daemon now also constructs the F4 Reconciler (main.rs:203), calls reconciler.prepare_cycle (main.rs:311), reconciler.apply_action (main.rs:336), and reconciler.reconcile (main.rs:369), and includes the resulting restore_outcomes in ControlCycleEnvelope::build (main.rs:370-377); the test's assertions still pass because the F4 reconciler's typed restore outcomes are valid RestoreOutcome values that the envelope accepts. EXECUTED: 1 passed; 0 failed.",
  "Matching correlation across generated outputs: the test asserts the same correlation_id appears in status (human-readable), status.json (machine envelope), control-cycles.jsonl (history), and decisions.log (text audit). The historical cycle (parsed from control-cycles.jsonl) equals the current cycle (parsed from status.json) byte-for-byte via assert_eq!(historical, cycle).",
  "Daemon JSON pass-through: f3_optctl_status_json_passthrough_uses_state_fallback_and_is_read_only (status_cli.rs) runs the real optctl binary (CARGO_BIN_EXE_optctl) against an isolated state dir with a dead D-Bus address (DBUS_SYSTEM_BUS_ADDRESS=unix:path=/definitely-missing-optid-system-bus), confirms optctl status --json returns the daemon-generated status.json byte-exact (no reconstruction from human text), and confirms the read-only contract (no state file is created or modified by the read). EXECUTED: 1 passed; 0 failed.",
  "Refusal to reconstruct JSON from human text: f3_optctl_status_json_rejects_malformed_and_missing_machine_state (status_cli.rs) confirms optctl status --json refuses to reconstruct machine status when status.json is malformed or absent, surfacing explicit error messages rather than silently synthesizing JSON from the human 'status' file. EXECUTED: 1 passed; 0 failed.",
  "Backward-compatible human status: f3_optctl_human_status_remains_backward_compatible_and_is_read_only (status_cli.rs) confirms optctl status (human mode) preserves the prior output contract (now prefixed with the cycle correlation_id) and remains read-only. EXECUTED: 1 passed; 0 failed.",
  "Schema and golden-fixture compatibility: the production_cycle test asserts status.json parses as ControlCycleEnvelope with schema_version == ENVELOPE_SCHEMA_VERSION (2). The golden fixture crates/optid/tests/fixtures/f3-control-cycle-v2.json (schema_version 2, correlation_id 'cycle-golden') is referenced by the optctl passthrough test and the envelope module tests validate golden compatibility and prohibited-marker rejection at the JSON level.",
  "Public diagnostic redaction: crates/optid/src/envelope.rs ControlCycleEnvelope::validate_schema rejects non-matching schema_version, empty correlation_id, and any public JSON containing prohibited markers /sys/, /proc/, /dev/, /home/, /root/, .rs: (also RUST_ and TOKEN= in the static marker list at envelope.rs:137). The production_cycle test's assertion that status.json parses as ControlCycleEnvelope transitively exercises this validation through the daemon's main.rs:379 envelope validation path. The F4 restore outcomes that now flow into the envelope (via main.rs:370-377) are typed RestoreOutcome values with sanitized target_id identifiers, so they do not introduce prohibited path markers.",
  "F4 restore outcomes integrated into the F3 envelope: main.rs:370-377 calls ControlCycleEnvelope::build(correlation_id, &snapshot, &decision, &boot_state, action_outcomes, restore_outcomes.clone()) where restore_outcomes is the Vec<RestoreOutcome> returned by reconciler.reconcile(&mut actuator)? at main.rs:369. envelope.rs:723 stores them as pub(crate) restore_outcomes: Vec<RestoreOutcome>. The production_cycle test transitively exercises this path because the daemon constructs the reconciler and calls reconcile() every cycle, even when no targets need restoration (the returned vec is empty, which the envelope accepts).",
  "Containing optid crate suite: cargo test --workspace executed 519 tests (optid lib 6 + optid main 421 + optctl main 9 + optctl status_cli 3 + rushbench 13 + testos lib 51 + testos-ingest 16), 0 failed, 0 ignored. The six F3 in-crate tests are part of the 421 optid main tests.",
  "Containing optctl suite: cargo test --test status_cli executed 3 tests, 0 failed, 0 ignored, finishing in 0.01s. The three F3 optctl tests are the entirety of the status_cli integration binary.",
  "Workspace-wide baseline: cargo fmt --all -- --check exit 0; cargo clippy --workspace --all-targets -- -D warnings exit 0; cargo check --workspace --all-targets --all-features exit 0.",
  "Repo tooling baseline: python3 tools/validate-current-work.py OK; python3 tools/validate-optid-packages.py PASS; python3 tools/validate-optid-packages.py --base bc6d974857a69da88795db8feb6d3f8cb75b5e21 PASS; python3 tools/render-frontpage.py --check OK; bash tools/finish-work.sh --dry-run OK; bash tools/checks.sh --ci --changed-base bc6d974857a69da88795db8feb6d3f8cb75b5e21 exit 0.",
]

# Source inspection performed (NOT runtime proof by itself; preserved for
# traceability). The runtime proofs above are the executable evidence.
source_inspection_only = [
  "crates/optid/src/tests.rs: all six ledger-mapped F3 in-crate acceptance tests exist as real '#[test] fn' definitions: f3_production_daemon_run_writes_one_correlated_versioned_cycle (mod f3_production_surface, tests.rs:148), f3_gate_matrix_distinguishes_dry_run_observe_off_and_boot_disarm, f3_actuator_matrix_reports_missing_unsupported_contract_and_allowlist_denials, f3_actuator_matrix_reports_success_failure_readback_drift_and_redundancy, f3_restore_matrix_reports_actual_success_failure_malformed_and_not_applicable, f3_observation_matrix_distinguishes_malformed_and_permission_denied_reads (mod f3_outcome_matrix, tests.rs:266).",
  "crates/optctl/tests/status_cli.rs: three integration tests run the real optctl binary (CARGO_BIN_EXE_optctl) against an isolated state dir with a dead D-Bus address.",
  "crates/optid/src/envelope.rs: ENVELOPE_SCHEMA_VERSION=2; ControlCycleEnvelope::validate_schema; the golden fixture crates/optid/tests/fixtures/f3-control-cycle-v2.json is committed (schema_version 2, correlation_id 'cycle-golden').",
  "crates/optid/src/dbus.rs: the StatusJson D-Bus method reads status.json from the daemon state directory and returns the daemon-generated JSON string.",
  "crates/optid/src/main.rs (post-F4 cutover): the daemon loop still builds one ControlCycleEnvelope per iteration (main.rs:370-377) with a boot-scoped CycleIdGenerator correlation ID, calls validate_schema/to_pretty_json/to_json_line, writes human 'status' (prefixed with correlation_id), 'status.json' (atomic), appends 'control-cycles.jsonl', and now drives restore outcomes from the F4 reconciler (reconciler.reconcile at main.rs:369) rather than from the legacy active_keys + Actuator::revert_key_outcome path. The legacy path is link-checked only (main.rs:88-101) and is no longer a production executor.",
]

# All previously-blocking environment limitations have been resolved in this
# verifier session. No unresolved findings remain.
unresolved = []

[historical_receipt.blocked_2026_08_01]
implementation_pr = 369
verified_commit = "107a4330e2d0505a6b48b35c7d4148482a426f3d"
verification_pr = 370
status = "blocked"
reason = "Prior verifier session on 2026-08-01 recorded result='blocked' against the same verified_commit because the verification sandbox could not execute the Rust toolchain (no cargo/rustc; static.rust-lang.org and crates.io unreachable; rustup toolchain download failed). No runtime proof existed. F1 was also blocked, so the F3 dependency gate could not be satisfied. A later verifier session (Arena.ai, PR #370, 2026-08-02) installed rustup stable (rustc 1.97.1), executed all nine F3 acceptance tests successfully, and independently verified F1 first; the blocked attempt is retained here as historical provenance."

# Prior post-#369 executable receipt (Arena.ai verifier, PR #370, 2026-08-02).
# Invalidated by PR #371's F4 cutover of crates/optid/src/main.rs and the
# extended production control-cycle restore path. Preserved here in full per
# the prompt's requirement to "retain prior invalidated or historical receipt
# information" when refreshing receipts.
[historical_receipt.post_369_arena_370]
implementation_pr = 369
verified_commit = "107a4330e2d0505a6b48b35c7d4148482a426f3d"
verification_pr = 370
status = "invalidated"
reason = "PR #371 modified the shared production entry point crates/optid/src/main.rs and extended the production control-cycle restore path after this verification (driving restoration through the F4 Reconciler and including F4 restore outcomes in the versioned envelope), truthfully demoting F3 to merged_incomplete until a fresh cold verifier re-ran the mapped F3 tests and versioned production envelope against a commit containing the F4 cutover. The current receipt above is that fresh re-verification at 52919c2ac1582e56737d568a576899e10ae43d15."
prior_verifier = "Independent cold-verification agent session (Arena.ai agent on branch arena/019fbea1-rush-linux, PR #370), separate from the implementation builder; rustc 1.97.1 (8bab26f4f 2026-07-14), cargo 1.97.1, executed 2026-08-02"
prior_result = "pass"
prior_totals = "cargo test --workspace: 517 passed, 0 failed, 0 ignored. All six mapped F3 in-crate acceptance tests and all three mapped F3 optctl status_cli integration tests passed individually with --exact."

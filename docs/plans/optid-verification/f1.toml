# Independent cold-verification receipt for:
# Freeze capability states and domain configuration
#
# Historical provenance (preserved, invalidated):
# - Original independent receipt: verified PR #332 at commit
#   001515bef45a0a5f9842d9831154ae9d0573d361. Invalidated because later
#   changes modified the declared F1 proof paths and removed the
#   receipt-referenced integration test.
# - Later fresh receipt: verified PR #332 at commit
#   6403938e07cd1cf793fddc7e08d237ac77b0b4fc. Invalidated because PR #369
#   modified the declared F1 proof paths (action.rs, actuator.rs, decision.rs,
#   main.rs, and policy.rs).
#
# This receipt records the independent cold-verification attempt against the
# current post-#369 implementation commit
# 107a4330e2d0505a6b48b35c7d4148482a426f3d (merge commit of PR #369), the
# exact commit that is HEAD of `main` for this run.
#
# RESULT IS NOT "pass". The independent verification sandbox could not
# execute the Rust toolchain at all: no cargo/rustc was installed, rustup
# was installed from the PyPI wheel, but the toolchain download from
# static.rust-lang.org and crate downloads from crates.io were unreachable
# from the sandbox network (see `unresolved`). No Rust acceptance test could
# be executed, so nothing in this receipt claims runtime proof. F1 remains
# `merged_incomplete` in the ledger; this receipt does NOT certify F1.

schema_version = 1
package = "F1"
implementation_pr = 332
# Commit under verification for this run: HEAD of main, merge commit of PR
# #369, which contains the F1 production surface that this receipt must cover.
# No test executed against it because the environment cannot run cargo.
verified_commit = "107a4330e2d0505a6b48b35c7d4148482a426f3d"
verifier = "Independent cold-verification agent session (Arena.ai agent on branch arena/019fbea1-rush-linux), separate from the implementation builder; executed 2026-08-01"
result = "blocked"

# Exact commands this verifier attempted, with their observed outcomes.
# Commands marked EXECUTED produced the stated result. Commands marked NOT
# EXECUTED could not run because no Rust toolchain is available in the
# verification environment.
commands = [
  "git fetch --all --prune; git checkout main; git reset --hard origin/main; git clean -fd; git rev-parse HEAD -> EXECUTED, HEAD=107a4330e2d0505a6b48b35c7d4148482a426f3d",
  "git merge-base --is-ancestor 107a4330e2d0505a6b48b35c7d4148482a426f3d HEAD -> EXECUTED, ancestor (exit 0)",
  "gh pr view 369 --repo Nan0pk/Rush-linux --json number,state,mergedAt,mergeCommit,title,url -> EXECUTED, state=MERGED, mergeCommit=107a4330e2d0505a6b48b35c7d4148482a426f3d",
  "command -v cargo; command -v rustc -> EXECUTED, not found (no Rust toolchain in image)",
  "rustup (1.29.0 via PyPI wheel rustup-1.29.0.1) -> EXECUTED, rustup installed; rustup default stable FAILED: could not download https://static.rust-lang.org/dist/channel-rust-stable.toml.sha256 (tls handshake eof)",
  "cargo fmt --all -- --check -> NOT EXECUTED (no toolchain; rustup shim errors: 'rustup could not choose a version of cargo to run, because one wasn't specified explicitly, and no default is configured.')",
  "cargo test --workspace -> NOT EXECUTED (same rustup toolchain error)",
  "cargo clippy --workspace --all-targets -- -D warnings -> NOT EXECUTED (same rustup toolchain error)",
  "cargo check --workspace --all-targets --all-features -> NOT EXECUTED (same rustup toolchain error)",
  "cargo test -p optid policy::f1_tests::f1_domain_mode_serde_accepts_lowercase_off_observe_actuate -- --exact -> NOT EXECUTED (no toolchain)",
  "cargo test -p optid policy::f1_tests::f1_domains_config_rejects_unknown_domain_key -- --exact -> NOT EXECUTED (no toolchain)",
  "cargo test -p optid policy::f1_tests::f1_systemd_set_property_is_domain_gated_by_default -- --exact -> NOT EXECUTED (no toolchain)",
  "cargo test -p optid policy::f1_tests::f1_decide_observe_mode_captures_would_be_action_for_render -- --exact -> NOT EXECUTED (no toolchain)",
  "cargo test -p optid policy::f1_tests::f1_decide_off_mode_suppresses_domain_actions_silently -- --exact -> NOT EXECUTED (no toolchain)",
  "cargo test -p optid policy::f1_tests::f1_decision_render_includes_effective_config_block -- --exact -> NOT EXECUTED (no toolchain)",
  "cargo test -p optid policy::f1_tests::f1_domain_default_mode_is_fail_closed_for_unknown_domains -- --exact -> NOT EXECUTED (no toolchain)",
  "cargo test -p optid policy::f1_tests::f1_production_pipeline_policy_to_render -- --exact -> NOT EXECUTED (no toolchain)",
  "cargo test -p optid policy::f1_tests::f1_production_pipeline_rejects_unknown_domain -- --exact -> NOT EXECUTED (no toolchain)",
  "cargo test -p optid -> NOT EXECUTED (no toolchain)",
  "python3 tools/validate-current-work.py -> EXECUTED, OK",
  "python3 tools/validate-optid-packages.py -> EXECUTED, PASS",
  "python3 tools/validate-optid-packages.py --base origin/main -> EXECUTED, PASS",
  "python3 tools/render-frontpage.py --check -> EXECUTED, OK",
  "bash tools/finish-work.sh --dry-run -> EXECUTED, 'Nothing to finish: the working tree is clean.'",
  "bash tools/checks.sh --ci --changed-base origin/main -> EXECUTED, all runnable sections passed; BLOCKED on missing 'pwsh' (policy section, R4/R8, exit 127)",
]

# NO runtime proofs exist for this run: the acceptance tests could not be
# executed. This list is intentionally empty; nothing below is a runtime
# claim.
runtime_proofs = []

# Source inspection performed this run (NOT runtime proof). The verifier
# read the current implementation at the verified commit and confirmed the
# mapped acceptance tests exist as real #[test] fns and that the declared
# production paths consume them. This is inspection evidence only; it does
# not certify behavior.
source_inspection_only = [
  "crates/optid/src/policy.rs: all nine ledger-mapped F1 acceptance tests exist as real '#[test] fn' definitions inside 'mod f1_tests' (policy.rs:1518): f1_domain_mode_serde_accepts_lowercase_off_observe_actuate, f1_domains_config_rejects_unknown_domain_key, f1_systemd_set_property_is_domain_gated_by_default, f1_decide_observe_mode_captures_would_be_action_for_render, f1_decide_off_mode_suppresses_domain_actions_silently, f1_decision_render_includes_effective_config_block, f1_domain_default_mode_is_fail_closed_for_unknown_domains, f1_production_pipeline_policy_to_render, f1_production_pipeline_rejects_unknown_domain.",
  "crates/optid/src/policy.rs: DomainMode serializes only as lowercase 'off' | 'observe' | 'actuate' (as_str at policy.rs:72-74); f1_domain_mode_serde_rejects_invalid_strings and f1_domain_mode_serde_rejects_uppercase_variants enforce strict parsing; DomainMode derives PartialOrd with Off < Observe < Actuate.",
  "crates/optid/src/policy.rs: DomainsConfig uses #[serde(deny_unknown_fields)] (policy.rs:239, 277), so an unknown [domains.<key>] table or unknown field is a parse error, not a silent default.",
  "crates/optid/src/policy.rs: SystemdSetProperty is mapped to Domain::CgroupReweight (policy.rs:84-98 repair note), so it is subject to the same per-domain gate as every other lever; no domain bypass path exists.",
  "crates/optid/src/policy.rs: EffectiveConfig::mode_for returns DomainMode::Off for unknown/future domains (policy.rs:356-361) — fail-closed default.",
  "crates/optid/src/policy.rs + crates/optid/src/decision.rs: f1_production_pipeline_policy_to_render exercises EffectiveConfig::from_policy -> decide_resolved -> Decision::render through production functions; f1_production_pipeline_rejects_unknown_domain exercises rejection through toml::from_str.",
  "crates/optid/src/main.rs: the daemon loop consumes the same Policy::load -> decide_resolved -> Decision::render path the F1 tests target.",
  "PR #369 diff: F1 declared proof paths action.rs, actuator.rs, decision.rs, main.rs, policy.rs were modified by PR #369, confirming the ledger's stale-receipt reasoning.",
]

unresolved = [
  "Environment blocker (this run): the verification sandbox has no Rust toolchain and cannot obtain one. rustup 1.29.0 was installed from the PyPI wheel, but static.rust-lang.org, static.crates.io, index.crates.io, crates.io, and archive.rust-lang.org are unreachable from the sandbox egress (only github.com, api.github.com, codeload.github.com, pypi.org, files.pythonhosted.org, and registry.npmjs.org are reachable). No Linux rustc/cargo toolchain wheel exists on PyPI or npm (verified by searching the full PyPI simple index and the npm registry). Building rustc from source is infeasible in this sandbox (2 vCPU, 3.8 GiB RAM, 20 GiB disk; the 1.54->1.61->1.68->1.75->1.80->1.85 bootstrap chain would take 20+ hours and exceed resources).",
  "pwsh is unavailable in the sandbox (no network route to Microsoft/GitHub release assets), so 'bash tools/checks.sh --ci --changed-base origin/main' cannot complete its policy section locally; all other sections passed.",
  "Consequence: F1 acceptance tests were NOT executed. No runtime proof exists. F1 must remain merged_incomplete until a cold verifier with a working Rust toolchain executes the mapped tests against a commit containing PR #369.",
  "The GitHub CI runs on the verification branch exercise the repository checks on GitHub runners (which have Rust and pwsh) and are inspected separately; CI results are not a substitute for the independent local cold run and are recorded in the verification PR.",
]

[historical_receipt]
implementation_pr = 332
verified_commit = "6403938e07cd1cf793fddc7e08d237ac77b0b4fc"
verification_pr = ""
status = "invalidated"
reason = "PR #369 modified the declared F1 proof paths (action.rs, actuator.rs, decision.rs, main.rs, policy.rs), so the prior fresh receipt no longer covers the current branch. Retained as historical provenance only."

[historical_receipt.original]
implementation_pr = 332
verified_commit = "001515bef45a0a5f9842d9831154ae9d0573d361"
verification_pr = 335
status = "invalidated"
reason = "Later changes modified declared F1 proof paths and removed the original receipt-referenced integration test."

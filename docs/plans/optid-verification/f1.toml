# Independent cold-verification receipt for:
# Freeze capability states and domain configuration
#
# Historical provenance:
# - The original independent receipt verified PR #332 at commit
#   001515bef45a0a5f9842d9831154ae9d0573d361.
# - Later PRs modified F1's declared production and evidence paths and deleted
#   the receipt-referenced integration test, so that receipt was correctly
#   invalidated and retained only as historical evidence.
# - This fresh receipt verifies the current post-repair surface at commit
#   6403938e07cd1cf793fddc7e08d237ac77b0b4fc.
#
# Verification was performed by a separate GLM 5.2 agent session. The verifier
# did not modify production or test code.

schema_version = 1
package = "F1"
implementation_pr = 332
verified_commit = "6403938e07cd1cf793fddc7e08d237ac77b0b4fc"
verifier = "GLM 5.2 independent cold-verification agent (separate session from the implementation builder)"
result = "pass"

commands = [
  "cargo fmt --all -- --check",
  "cargo test -p optid",
  "cargo test --workspace",
  "cargo clippy --workspace --all-targets -- -D warnings",
  "cargo check --workspace --all-targets --all-features",
  "cargo test -p optid policy::f1_tests::f1_domain_mode_serde_accepts_lowercase_off_observe_actuate -- --exact",
  "cargo test -p optid policy::f1_tests::f1_domains_config_rejects_unknown_domain_key -- --exact",
  "cargo test -p optid policy::f1_tests::f1_systemd_set_property_is_domain_gated_by_default -- --exact",
  "cargo test -p optid policy::f1_tests::f1_decide_observe_mode_captures_would_be_action_for_render -- --exact",
  "cargo test -p optid policy::f1_tests::f1_decide_off_mode_suppresses_domain_actions_silently -- --exact",
  "cargo test -p optid policy::f1_tests::f1_decision_render_includes_effective_config_block -- --exact",
  "cargo test -p optid policy::f1_tests::f1_domain_default_mode_is_fail_closed_for_unknown_domains -- --exact",
  "cargo test -p optid policy::f1_tests::f1_production_pipeline_policy_to_render -- --exact",
  "cargo test -p optid policy::f1_tests::f1_production_pipeline_rejects_unknown_domain -- --exact",
  "python3 tools/validate-current-work.py",
  "python3 tools/validate-optid-packages.py",
  "python3 tools/validate-optid-packages.py --base origin/main",
  "python3 tools/render-frontpage.py --check",
  "bash tools/finish-work.sh --dry-run",
]

runtime_proofs = [
  "crates/optid/src/policy.rs: policy::f1_tests::f1_domain_mode_serde_accepts_lowercase_off_observe_actuate proves strict lowercase parsing of off, observe, and actuate.",
  "crates/optid/src/policy.rs: policy::f1_tests::f1_domains_config_rejects_unknown_domain_key proves unknown domain configuration fails during the real TOML parse path rather than silently defaulting.",
  "crates/optid/src/policy.rs and crates/optid/src/action.rs: policy::f1_tests::f1_systemd_set_property_is_domain_gated_by_default proves SystemdSetProperty cannot bypass domain gating.",
  "policy::f1_tests::f1_decide_observe_mode_captures_would_be_action_for_render proves observe mode retains the action that would have been attempted while suppressing actuation.",
  "policy::f1_tests::f1_decide_off_mode_suppresses_domain_actions_silently proves off mode suppresses the domain's actions.",
  "crates/optid/src/decision.rs: policy::f1_tests::f1_decision_render_includes_effective_config_block proves effective configuration reaches rendered runtime status.",
  "policy::f1_tests::f1_domain_default_mode_is_fail_closed_for_unknown_domains proves a future or unknown domain cannot silently default to actuation.",
  "policy::f1_tests::f1_production_pipeline_policy_to_render exercises EffectiveConfig::from_policy through decide_resolved to Decision::render using the production functions.",
  "policy::f1_tests::f1_production_pipeline_rejects_unknown_domain exercises rejection through the production toml::from_str configuration path.",
  "The verifier inspected the declared F1 runtime entrypoints and confirmed that the daemon consumes the same policy and rendering path exercised by the tests; no pointer-only test or dormant duplicate is counted as integration evidence.",
  "The verification branch changed only the receipt, package ledger, current-work projection, and generated README; no production or test source was changed.",
]

unresolved = []

[historical_receipt]
implementation_pr = 332
verified_commit = "001515bef45a0a5f9842d9831154ae9d0573d361"
verification_pr = 335
status = "invalidated"
reason = "Later changes modified declared F1 proof paths and removed the original receipt-referenced integration test."

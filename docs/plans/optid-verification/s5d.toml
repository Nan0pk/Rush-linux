# Independent cold-verification receipt for S5D.
# Previous receipts and builder recertifications remain preserved in Git history.

schema_version = 1
package = "S5D"
implementation_pr = 392
verification_pr = 447
verified_commit = "b673c0e185639c556a69e88e806ed11946f526d1"
verifier = "Independent ChatGPT GitHub verifier; GitHub Actions ubuntu-24.04"
result = "pass"
workflow_run = 33332810449
workflow_job = 99314354620

commands = [
  "PR Gate run 33332810449 job 99314354620: cargo fmt --all -- --check -> PASS",
  "PR Gate run 33332810449 job 99314354620: cargo test --workspace -> PASS; optid main 548 passed",
  "PR Gate run 33332810449 job 99314354620: cargo test -p optid --features test-simulation --test i2_simulation_evidence_cli -> PASS; 6 passed",
  "PR Gate run 33332810449 job 99314354620: cargo clippy --workspace --all-targets -- -D warnings -> PASS",
  "PR Gate run 33332810449 job 99314354620: cargo check --workspace --all-targets --all-features -> PASS",
  "PR Gate run 33332810449 job 99314354620: python3 tools/validate-optid-packages.py --base 99069058ede92065083c39e408b7803b6aca0f8c -> PASS",
]

runtime_proofs = [
  "All thirteen mapped S5D acceptance tests passed: persistent scoped opening, restart persistence, recovery-before-canary, canary success and failure, firmware scoping, root authorization, backward-clock safety, multi-domain isolation, restore-failure scoping, process-wide corruption fallback, production diagnostics, and one-shot clear command behavior.",
  "Repeated attributable failures quarantined only the affected scope, persisted across restart, required recovery before one canary, closed only after confirmed success, and reopened immediately on canary failure; backward clock movement could not shorten cooldown.",
  "Restore failures remained domain-scoped while unisolatable process corruption forced global observe-only; production diagnostics included scope and firmware identity, and manual clear remained root-authorized and one-shot.",
  "The exact verified commit differs from main only by the temporary rush_collect CI-selection test and changes no S5D production, test, service, policy, safety threshold, packaging, or declared proof path.",
]

unresolved = []

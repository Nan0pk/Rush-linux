# Rush Linux Root Partition
#
# ext4 root filesystem containing the common OS plus installed edition
# system-extension artifacts under /var/lib/extensions.
# Minimize=yes strips unnecessary files from the image.

[Partition]
Type=root
Format=ext4
Label=RushRoot
CopyFiles=/
# Minimize=yes
SizeMinBytes=4G

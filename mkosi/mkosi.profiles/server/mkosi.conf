# Rush Linux Server Edition — mkosi profile
#
# Headless cgroup-v2 server profile with nftables, optid, and
# zero desktop packages. This is the v0.5.0-beta.1 target.
#
# Criterion 4: "server edition has no desktop dependency"

[Output]
ImageId=rush-linux-server

[Content]
# No desktop packages. Base + kernel + headless services only.
# The base packages from the parent mkosi.conf are inherited.
# Add only server-specific packages here.
# Packages=
#     # networking
#     # container runtime support
#     # (no plasma, no wayland, no pipewire, no x11)

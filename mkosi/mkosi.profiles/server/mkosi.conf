# Rush Linux Server Edition — metadata-only edition extension.
#
# The single base image is already the headless server payload. This profile is
# intentionally empty: tools/build-edition-sysext.py still emits the signed
# edition identity/manifest extension while adding no desktop packages.

[Output]
ImageId=rush-linux-server

[Content]
Packages=

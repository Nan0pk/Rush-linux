# Rush Linux — Session Handoff Prompt (supersedes HANDOFF.md 2026-06-20)

> **START HERE — Dragnet:** run `python3 tools/dragnet.py --observe` and read
> the newest `release/evidence/dragnet/` report first. As of 2026-06-23 the
> Dragnet-001 evidence debt is **closed** — PR #174 committed all outstanding
> v0.3/v0.4/v0.5 build-host transcripts. Dragnet-004 (2026-06-26) is the first
> GREEN report. The notes in `HANDOFF.md` (the older file) describe the
> pre-PR-#174 state and are kept only for history.

**Project:** Rush Linux — a source-built Linux distribution centered on
`optid`, a fast, explainable runtime optimizer.
**Repository:** https://github.com/Nan0pk/Rush-linux
**Current Version:** `0.5.0-beta.1` (Minimal Installable System — complete)
**Next Version:** `0.6.0-beta.1` (Hardware-Aware optid — see
`docs/plans/v0.6-hardware-aware-optid-proposal.md`)
**Date:** 2026-06-26

## What Was Done This Session (2026-06-26)

1. Confirmed v0.5.0-beta.1 is genuinely complete on disk:
   `release/evidence/v0.{3,4,5}*/{c1,c2,c3,c4}/` all carry `meta.txt` +
   `transcript.log`. PR #174 (merged 2026-06-23) closed the Dragnet-001 debt.
   Dragnet-004 (2026-06-26) is the first GREEN report.
2. Produced the v0.6 implementation proposal at
   `docs/plans/v0.6-hardware-aware-optid-proposal.md` (5-phase plan mirroring
   the v0.5 proposal; Phase A is hardware-allowlist foundation).
3. Reconciled doc drift in `README.md`, `ROADMAP.md`, `RELEASES.md`,
   `release/evidence/dragnet/LEDGER.md` (this PR).
4. Closed issue #168 (Dragnet evidence debt) with a comment linking PR #174.
5. Posted a strategic verdict on issue #105 (Strategic Reassessment) —
   on-track, course-corrections: nominate two reference machines for v0.6
   Phase D; start v0.6 Phase A as soon as the proposal lands.

## What Needs To Be Done

### Immediate (next session)

1. Merge the v0.6 proposal PR.
2. Open v0.6 Phase A as the first Builder Work Package —
   `config/optid/hardware-allowlist.toml` schema + `crates/optid/src/platform/mod.rs`
   + gate every actuator write behind allowlist check. In-container; no
   hardware needed.
3. Bump VERSION → `0.6.0-beta.1` once Phase A is verified. Mirror PR #157's
   14-file pattern.

### v0.6 critical path

```
Phase A (allowlist) → Phase B (shims) → Phase C (foreground/vm.guest) →
Phase D (reference machines + baselines) → Phase E (docs + bump to 0.7)
```

Phase D is the hard gate — it needs two physical machines and the project
owner's hands. Surface this in the Phase A PR description so D1 (machine
nomination) starts in parallel.

### Open PRs

None as of 2026-06-26. (PR #159 and PR #160 mentioned in the older HANDOFF
are no longer in the open-PR list — verify they were merged, not closed
without merge.)

## Rules of Engagement

Unchanged from `docs/AI_CONTINUATION.md`. Builder/Verifier split is mandatory;
no self-certification; `start-work.sh` / `finish-work.sh` lifecycle; modern
Linux defaults; one adaptive policy owner (`optid`).

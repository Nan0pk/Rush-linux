# WP-<NAME> (PR #N) — VERIFIER VERDICT: <ACCEPT|CONDITIONAL PASS|REJECT|DEFER>

**Branch**: `<branch>` @ `<sha>`
**PR**: #N
**Tracks**: <issue>
**Verdict by**: <role> (<session>), <date>

## Evidence (<source-of-truth>)
- <one-line per concrete fact verifier checked, with SHA / file path>

## Gate: <OPEN|CLOSED>
<one-paragraph: cite SPEC section if applicable, name both gates if relevant>

## Caveats (numbered, never vague)
1. <caveat with file/commit pointer>
2. ...

## Cross-checks performed
1. <verifier action — independent download / git show / curl / json parse>
2. ...

## Next
- HUMAN: <one-line>
- BUILDER: <one-line>
- Verifier: <one-line>

Baton → <role>. Verdict by <role>, <date>.

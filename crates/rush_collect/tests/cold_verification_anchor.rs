#[test]
fn cold_verification_anchor() {
    // Temporary PR-only anchor: forces the full Rust workspace CI lane while
    // leaving every optid production, test, and declared proof path unchanged.
    assert!(true);
}

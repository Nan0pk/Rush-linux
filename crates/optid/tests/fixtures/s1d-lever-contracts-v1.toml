schema_version = 1

[[lever]]
id = "cpu_dma_pm_qos"
implementation = "current"
stable_identity = "optid descriptor owner"
supported_abi = "/dev/cpu_dma_latency request descriptor"
credible_worst_case = "severe power loss or leaked latency request"
semantic_envelope = "release or non-negative i32 microseconds"
original_capture = "descriptor ownership"
ownership_drift_rule = "only optid-owned descriptors may be closed"
rollback = "close the owned request descriptor"
stabilization = "close all optid-owned request descriptors and quarantine"
verification = "descriptor ownership and effective constraint observation"
unsupported_behavior = "deny actuation; permit observation and explanation"

[[lever]]
id = "device_pm_qos"
implementation = "current"
stable_identity = "device HWID and firmware"
supported_abi = "power/pm_qos_resume_latency_us"
credible_worst_case = "device unresponsiveness or contract violation"
semantic_envelope = "release or non-negative i32 microseconds"
original_capture = "captured request value or owned-request absence"
ownership_drift_rule = "compare last confirmed value and relinquish on drift"
rollback = "restore captured request or remove only optid-owned request"
stabilization = "relax the owned request or force the device active"
verification = "readback and unchanged device identity"
unsupported_behavior = "deny actuation; permit observation and explanation"

[[lever]]
id = "cpu_epp"
implementation = "current"
stable_identity = "CPU policy"
supported_abi = "energy_performance_preference and advertised choices"
credible_worst_case = "performance loss, heat, noise, or battery drain"
semantic_envelope = "advertised standard EPP tokens only"
original_capture = "captured startup value for every policy"
ownership_drift_rule = "compare each last confirmed policy value and relinquish on drift"
rollback = "restore each captured startup value"
stabilization = "use an advertised balanced preference"
verification = "read every affected CPU policy back"
unsupported_behavior = "deny actuation; permit observation and explanation"

[[lever]]
id = "platform_profile"
implementation = "current"
stable_identity = "platform firmware"
supported_abi = "ACPI platform_profile and platform_profile_choices"
credible_worst_case = "severe throttling, heat, or fan noise"
semantic_envelope = "advertised known profile tokens only"
original_capture = "captured advertised startup profile"
ownership_drift_rule = "compare last confirmed profile and relinquish on drift"
rollback = "restore captured advertised profile"
stabilization = "use balanced only when advertised"
verification = "readback and advertised-choice check"
unsupported_behavior = "deny actuation; permit observation and explanation"

[[lever]]
id = "runtime_pm"
implementation = "current"
stable_identity = "device HWID and firmware"
supported_abi = "power/control plus optional autosuspend_delay_ms"
credible_worst_case = "lost input, network, audio, graphics, or device hang"
semantic_envelope = "control=auto and delay 0 through 3600000 milliseconds"
original_capture = "captured control and delay"
ownership_drift_rule = "compare last confirmed pair and relinquish on drift"
rollback = "restore captured control and delay"
stabilization = "write power/control=on to force active"
verification = "readback and unchanged device identity"
unsupported_behavior = "deny actuation; permit observation and explanation"

[[lever]]
id = "sata_alpm"
implementation = "current"
stable_identity = "SCSI host and backing controller"
supported_abi = "link_power_management_policy"
credible_worst_case = "storage latency, timeout, or controller instability"
semantic_envelope = "known kernel ALPM policy tokens only"
original_capture = "captured host policy"
ownership_drift_rule = "compare last confirmed policy and relinquish on drift"
rollback = "restore captured host policy"
stabilization = "use max_performance when supported"
verification = "readback on the same SCSI host identity"
unsupported_behavior = "deny actuation; permit observation and explanation"

[[lever]]
id = "pcie_aspm"
implementation = "current"
stable_identity = "PCI function and topology"
supported_abi = "link/l1_aspm boolean"
credible_worst_case = "PCIe timeout, endpoint instability, or device loss"
semantic_envelope = "boolean enable or disable only"
original_capture = "captured link state"
ownership_drift_rule = "compare last confirmed state and relinquish on drift"
rollback = "restore captured link state"
stabilization = "disable only the optid-enabled deeper state"
verification = "readback and PCI identity/topology match"
unsupported_behavior = "deny actuation; permit observation and explanation"

[[lever]]
id = "backlight"
implementation = "current"
stable_identity = "selected display panel"
supported_abi = "brightness and max_brightness"
credible_worst_case = "visually unusable display"
semantic_envelope = "10 through 100 percent with hardware floor required for stabilization"
original_capture = "captured user-owned raw brightness"
ownership_drift_rule = "compare user-owned last confirmed value and relinquish on drift"
rollback = "restore captured raw brightness"
stabilization = "use a hardware-verified visibility floor"
verification = "readback and selected-panel identity"
unsupported_behavior = "deny actuation; permit observation and explanation"

[[lever]]
id = "vm_sysctl"
implementation = "current"
stable_identity = "recorded boot policy"
supported_abi = "approved /proc/sys/vm integer knobs"
credible_worst_case = "OOM pressure, writeback stalls, or responsiveness loss"
semantic_envelope = "approved knob-specific integer ranges"
original_capture = "captured boot/startup values"
ownership_drift_rule = "compare every transaction member and relinquish on drift"
rollback = "restore every captured boot/startup value"
stabilization = "use recorded distribution boot policy only with provenance"
verification = "readback of every transaction member"
unsupported_behavior = "deny actuation; permit observation and explanation"

[[lever]]
id = "powercap_pl1"
implementation = "planned"
stable_identity = "CPU package and firmware"
supported_abi = "powercap constraint"
credible_worst_case = "throttling, heat, charger stress, or unstable package power"
semantic_envelope = "1 through 100 percent of reviewed hardware limit"
original_capture = "captured firmware/startup limit"
ownership_drift_rule = "compare last confirmed limit and relinquish on drift"
rollback = "restore captured firmware/startup limit"
stabilization = "stop writing or use a reviewed conservative cap"
verification = "bounds, readback, package identity, and telemetry"
unsupported_behavior = "deny actuation; permit observation and explanation"

[[lever]]
id = "dgpu_runtime_pm"
implementation = "planned"
stable_identity = "GPU HWID, driver, and firmware"
supported_abi = "power/control plus optional autosuspend_delay_ms; MUX excluded"
credible_worst_case = "GPU loss, display disruption, driver hang, or failed resume"
semantic_envelope = "control=auto and delay 0 through 3600000 milliseconds"
original_capture = "captured control and delay"
ownership_drift_rule = "compare last confirmed pair and relinquish on drift"
rollback = "restore captured control and delay"
stabilization = "write power/control=on while leaving MUX untouched"
verification = "readback and GPU/driver/firmware identity"
unsupported_behavior = "deny actuation; permit observation and explanation"
